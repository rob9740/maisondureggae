{% extends 'base.html.twig' %}

{% block title %}Hello CartController!{% endblock %}

{% block body %}

<h1>panier</h1>

{% if not panier %}

{% if panier %}
    votre panier est vide (alert ici)
{% endif %}

<table class="table table-hover">
        <thead>
            <tr>
            <th scope="col">#</th>
            <th scope="col">First</th>
            <th scope="col">Last</th>
            <th scope="col">Handle</th>
            </tr>
        </thead>
    <tbody>

  {% for element in panier.elements %}
  
    <tr>
      <th scope="row">{{ element.musique.titre }}</th>
      <td>{{ element.quantity }}</td>
      <td>{{ element.musiques.prix }}</td>
    </tr>
    
  {% endfor %}

  <tr>
  <td></td>
  <td>Total</td>
  <td>{{ panier.total }}</td>
  </tr>
</table>

<a type="button" class="btn-btn-primary" href="{{ path('app_cart_clear') }}">vider mon panier</button>

{% endif %}
{% endblock %}
